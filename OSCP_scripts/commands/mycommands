#!/bin/bash
cat <<'EOF'
Available custom commands:

- addhost <IP> <hostname1> [hostname2] ...
    Add/update entries in /etc/hosts.

- auth_ldap <IP> <username> <password> <domain> [-ldap|-ldaps]
    Authenticated LDAP dump + info/UAC/cascadeLegacy extraction.

- auth_smb -t <IP> -u <USER> -p <PASS> [-d <DOMAIN>]
    Authenticated SMB/LDAP enum, DPAPI, and NetExec modules.
    Note: omitting -d means local auth.

- noauth_ldap -t <IP> -d <domain> [-ldaps]
    Anonymous LDAP enum; saves usernames and cascadeLegacy creds.

- noauth_smb <IP>
    Anonymous SMB enum (nmblookup, nmap SMB, rpcclient, RID brute, modules, pulls).

- noauth_kerberos -t <DC_IP> -d <DOMAIN>
    Kerbrute userenum or AS-REP roasting (GetNPUsers).

- dns_enum <DNS_IP> <domain> [subdomains.txt]
    DNS/AD SRV queries, AXFR attempt, reverse, optional brute-force.

- lin_stable_shell
    TTY upgrade and terminal tweaks cheat-sheet.
EOF
